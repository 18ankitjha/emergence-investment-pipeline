Lua AI - The Agent OS Backed by Combinator The AI workforce that companies actually run on. One workspace where people and agents work together — same rooms, same memory, governed on every action. Schedule a demo Join the waitlist Built with the world's leading AI providers. No lock-in Deploy real AI agents that solve real problems Written in TypeScript by your engineers, or composed using our Agent Builder by everyone else — same platform, production-grade either way. 01 · Developers I'm a developer. Real TypeScript, not YAML. Write actual code with Zod validation, proper testing, and a modern dev workflow. Quick start 1 import { LuaTool } from "lua-cli" ; 2 import { z } from "zod" ; 3 4 export class CheckOrderStatus implements LuaTool { 5 name = "check_order_status" ; 6 - description = "Look up a customer’s order" ; + description = "Look up a customer’s order (yes, even that one)" ; 7 8 inputSchema = z. object ( { 9 orderId: z. string (), + politeness: z. enum ([ "nice" , "extra-nice" ]), 10 } ); 11 12 async execute ( { orderId } ) { 13 const order = await shopify.orders. get (orderId); 14 return { status: order.status, eta: order.eta } ; 15 } 16 } 02 · Everyone else Build without code. Create agents for support, sales, HR, research & more. No code needed. Deploy to mobile, web, Slack instantly. Start building Email SMS Voice WhatsApp More Your AI Agent 4 connections Active Chat with your agent Runs where your team already works. Salesforce Slack Your order shipped today — tracking number on the way. 9:41 PM SH Sara Haddad 9:42 AM did anyone follow up on the refund from #support ? Omar Farouk Re: Q3 board pack — looks great, one note on… 9:41 Refund · $89.00 re_3O2xk1 Succeeded ChatGPT Rewrite this refund email to sound friendlier Here's a warmer version — “Hi Amira, thanks for your patience…” Memory updated · 3 new facts Claude Opus 4.8 PDF Q3-board-pack.pdf 14 slides · 2.1 MB Q3 Board Pack Prepared by Lua · 14 slides Revenue closed the quarter at $4.2M, up 12% on Q2 and ahead of the reforecast. Churn held flat at 1.8% despite the pricing change. Two enterprise renewals slipped into October — both flagged below with owners and next steps. Meeting notes Product sync · Thursday 10:00 Activation gap traced to the admin-to-teammate handoff — Priya walked through the funnel breakdown. Decision: ship the invite rework first, revisit onboarding email cadence after. Daily brief Wednesday · from Lua 9:00 Standup — auth infra update expected 12:30 Design review moved to room 2 3:00 Board prep — deck is drafted, review by 2 LUA-447 Fix activation email sequence · Build and ship sequence for self-onboardings · Target accounts with no second user invited 0:32 9:42 PM The Agent OS The Agent OS Everything you need to work with production-ready agents. Introducing Lua Workspace The Agent OS — your team and its agents in one place. Chat is where the work happens — every agent, one conversation away. Features Suggested prompts @ agents & files Voice notes & calls Auto model routing Tools connect once and serve the whole workspace. Features Shared connections Per-team catalog One-click connect Owner-held control Tasks keep the standing work running — on schedule, without you. Features Recommended routines Scheduled runs Run now Last-run status Memory is a living map of the company that every agent draws on. Features Living entity graph Confidence-weighted edges Facts that decay Ask anything Agents for every team One platform underneath — each team gets agents tuned to its own work. Engineering Customer support Sales E-commerce HR send-invoice.ts export class SendInvoice implements LuaTool { name = "send_invoice" ; inputSchema = z. object ( { … } ); async execute (input) { … } } ✓ 12 tests passed · deployed in 4s Engineering Real TypeScript, not YAML. Write actual code with Zod validation, proper testing, and a modern dev workflow. Typed tools in TypeScript Define tools with Zod-validated inputs, test locally, deploy through Lua. Event-driven 