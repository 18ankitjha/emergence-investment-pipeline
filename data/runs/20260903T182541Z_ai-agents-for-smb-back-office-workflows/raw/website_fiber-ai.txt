Fiber AI | Integrations — SDKs, MCP, Webhooks & More We're #1 on OpenFunnel benchmarks for verified contact details & company resolution Solutions AI SDR Testimonials Docs Connect Fiber AI to your stack MCP, APIs, AI plugins, OpenFiber, and SDKs — pick the surface that fits your stack. Browse integrations The best companies grow with Fiber AI Surfaces Pick the surface that fits your stack One Fiber API, five ways to reach it. Switch tabs to compare what each surface gives your agent or app. MCP APIs AI Plugin OpenFiber SDKs Use cases Prompts that map to Fiber tools Pick a prompt and watch the agent call Fiber, read the response, and answer. Real responses from the Fiber MCP server, trimmed for display. coding agent Fiber MCP Try a prompt Find SaaS companies in New York with 50 to 200 employees Find AI companies in the Bay Area hiring engineers List US companies that raised a Series A recently Find Heads of Sales at fintech companies Find the LinkedIn profile for the GitHub user likedan Find the LinkedIn behind the X handle @garrytan Reveal the verified work email and phone for this profile Pull live LinkedIn data for stripe.com Check recent LinkedIn activity for linkedin.com/in/williamhgates Run Agent run Done Find SaaS companies in New York with 50 to 200 employees Setup Connect your agent Get a key, add the server, then wire auth. V3 skips the key — it signs you in with OAuth. V2 and Core only. Create one at fiber.ai/app/api, then export it before the Claude commands or .mcp.json below. V3 uses OAuth — skip this. API key export FIBER_API_KEY="YOUR_API_KEY" Add the server to your agent. Wire auth after install for V2 and Core. Cursor Claude Code .mcp.json Run in Terminal (macOS) or click Install. Wire your API key after for V2 and Core. V2 Recommended API key Best for most projects open 'cursor://anysphere.cursor-deeplink/mcp/install?name=fiber-ai-v2&config=eyJ0eXBlIjoiaHR0cCIsInVybCI6Imh0dHBzOi8vbWNwLmZpYmVyLmFpL21jcC92MiJ9' Install Then add x-api-key in Cursor MCP settings, or paste your key in chat. +2 more Endpoints Pick an endpoint Most people start with V2. Full setup for every client is in the docs. Recommended V2 Best for most projects https://mcp.fiber.ai/mcp/v2 API key The ~10 most-used operations exposed as direct tools, so agents pick the right one fast. Authenticated with an API key. V3 Best for full coverage https://mcp.fiber.ai/mcp/v3 OAuth Every public operation as a direct tool, with OAuth sign-in so there is no API key to manage in config. Core Best for a small tool list https://mcp.fiber.ai/mcp API key A few meta-tools that discover and call any operation on demand, which keeps the agent's tool list short. Compatibility Works with your stack Cursor Claude Code Gemini CLI Codex CLI GitHub Copilot CLI VS Code Windsurf + many more Stories from teams that run on our APIs Wanted to hire 4 senior PMs with law degrees from legal tech startups in SF/Seattle – a search impossible on LinkedIn Recruiter. Our people search APIs helped export the full lead list and they closed candidates the same quarter. Using our people search APIs , filtered list of all CFOs at US companies, 11-500 people, w >$1M revenue to run "matched audiences" ads on LinkedIn. Projected ROAS increase of XX% (NDA). Was unhappy with their yield on Clay tables, so used our search APIs to generate a XX% (NDA) larger lead list and enrich the contact information for them all. Also uses our live fetch APIs to find relevant LinkedIn posts, grabbing all people who commented and reacted, and enriching with contacts for retargeting. Uses our agentic search APIs to power their company search and people search AI Agents. We enabled very powerful filters for venture funding, open and closed job postings, revenue, technologies, linkedin profile keywords, education, etc. One of the World’s Largest Agentic AI Coding Platforms (NDA) Our people search APIs help them enrich profile data for current and waitlisted users to get all of the profile details on these peop