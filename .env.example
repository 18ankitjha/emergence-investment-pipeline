# Optional. If absent, the pipeline uses a clearly marked deterministic fallback analysis
# so reviewers can still inspect committed artifacts.
OPENAI_API_KEY=

# Optional model override.
OPENAI_MODEL=gpt-4.1-mini

